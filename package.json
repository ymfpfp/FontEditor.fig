{
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js",
    "watch": "npm run build -- --watch"
  },
  "type": "module",
  "author": "",
  "dependencies": {
    "font-engine": "file:./font-engine"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "esbuild": "^0.27.1",
    "typescript": "^5.3.2"
  }
}
